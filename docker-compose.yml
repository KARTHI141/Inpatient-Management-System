version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
      - PORT=8080
    restart: unless-stopped

  # Optional: Oracle database (requires Oracle license)
  # oracle-db:
  #   image: container-registry.oracle.com/database/express:21.3.0-xe
  #   ports:
  #     - "1521:1521"
  #   environment:
  #     - ORACLE_PWD=system
  #   volumes:
  #     - oracle-data:/opt/oracle/oradata

# volumes:
#   oracle-data:
